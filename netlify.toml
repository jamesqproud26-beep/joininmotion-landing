[build]
  command = "node ./scripts/write-supabase-config.js"
  publish = "."

[context.production.environment]
# Set SUPABASE_URL and SUPABASE_ANON_KEY in the Netlify UI (Site settings → Build & deploy → Environment)
